@{
  Layout = "_Layout";
}

<h1>All Contacts</h1>
@* @if (@Model.Count == 0)
{
  <h3>No contacts have been added yet.</h3>
}
<ol>
  @foreach (var contact in Model)
  {
    <h3>@contact.GetName()</h3>
  }
</ol> *@

<a href="/contact/add"><button>Add a new contact</button></a>
